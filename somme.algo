ALGORITHM phrase
VAR
    mot : STRING:= "BONJOUR.";
BEGIN
    Read(mot)
    FOR i  FROM i < mot.length TO i++ DO 
    mot[i]++;
    write(mot[i]);
END_FOR   
FOR n  FROM n < mot.length TO n++ DO
    write(mot[n]);
END_FOR 
END